{{#paper-button primary=true action='answerQuestionForm' }}Add Answer{{/paper-button}}
{{#if answerQuestionForm}}
  {{paper-input label="Username" value=user}}
  {{paper-input textarea=true label="Answer" value=reply}}
  {{#paper-button primary=true raised=true action='newAnswer'}}Save{{/paper-button}}
{{/if}}

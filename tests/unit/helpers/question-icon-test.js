import { questionIcon } from 'answer-forum/helpers/question-icon';
import { module, test } from 'qunit';

module('Unit | Helper | question icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionIcon([42]);
  assert.ok(result);
});
